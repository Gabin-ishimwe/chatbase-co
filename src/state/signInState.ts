export const initialState = {
  message: "",
  toastColor: false,
  toast: false,
};
type Action = {
  type: string;
  payload: any;
};

export const signInReducer = (initialState: any, action: Action) => {};
